import { Box } from '@mui/material';
import React from 'react';
import { Layout } from '../../common/layout/layout.component';

const StartProject = () => {
  return (
    <Layout>
      <Box maxWidth='xl'>Start Project</Box>
    </Layout>
  );
};

export default StartProject;
